#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n = 0;
   cin>>n;


    for(int i =0; i<n; i++){
    int value = 1;
    for(int z=0; z<=i; z++){
        cout<<value<<" ";
        value = value * (i - z) / (z + 1);

    }
    cout<<endl;
   }


   }


